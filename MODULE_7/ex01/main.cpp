#include "iter.hpp"

template <typename T>

void print_element(T& n) {
	std::cout << n << std::endl;
}

int main() {
	// int array[] = {1, 2, 3, 4, 5};
	// char arr[6] = {'a', '.', 'c', 'd', 'e'};
	// int n = sizeof(array) / sizeof(array[0]);

	// iter(array, n, print_element<int>);
	// iter(arr, n, print_element<char>);
	std::cout << "hello" << std::endl;
  return 0;
}
